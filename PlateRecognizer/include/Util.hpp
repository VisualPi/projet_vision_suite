#pragma once

class Util
{
public:
	/*cv::Mat DrawCountour( const Plate& plate, cv::Point2f* rect, cv::Scalar color, int thickness = 1, int lineType = 8, int shift = 0 )
	{
		cv::Mat result;
		cv::Point2f rect_points[4];
		plate.GetRect().points( rect_points );
		for ( int j = 0; j < 4; j++ )
			line( result, rect_points[j], rect_points[( j + 1 ) % 4], cv::Scalar( 255, 100, 0 ), 5, 8 );
		return result;
	}*/

	
};
